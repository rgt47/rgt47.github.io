---
title: "A simple function to generate a vector of character strings"
description: |
  I frequently find myself with a sequence of unquoted, whitespace se
  separated strings that I want to convert to a vector of character strings. 
What to do? 
author:
  - name: Ronald G. Thomas 
    url: https://inferenceX.com/rgthomas
    affiliation: UCSD and InfereceX
    affiliation_url: https://inferenceX.com
date: 06-30-2019
output:
  distill::distill_article:
    self_contained: false
---

```{r setup, include=FALSE}
library(pacman)
p_load(tibble, kableExtra, formatR, broom, snakecase, fs,forcats, gee,
       ggpubr, nlme,gridExtra,purrr, tidyr, ztable, readr, dplyr,
       magrittr, lubridate,survival, ggplot2, printr, lsmeans,
       ggthemes,knitr, xtable, doBy, Hmisc) 
opts_chunk$set(warning = F, message = F, echo=F, fig.width=5.2,
	       fig.height=5,results='asis', dev='pdf',
	       opts_chunk$set(cache.path = "cache/"))
source('~/projects/reports/templates/zz.tools.R')
source("../R_rainclouds.R")
options(scipen = 1, digits = 2, warn=-1)
options(ztable.type="latex")
pd <- position_dodge(0.1)
cbPalette <- c( "#8A3324","#848482", "#0072BB", "#009E73")
ctrl <- lmeControl(opt='optim')
# Learn more about creating blogs with Distill at:
# https://rstudio.github.io/distill/blog.html

```


# Introduction

  I frequently find myself with a sequence of unquoted, whitespace se
  separated strings that I want to convert to a vector of character strings. 
My usual goto function to accomplish this is the Cs() function in the
Hmisc library, but this very useful function doesn't always do the
